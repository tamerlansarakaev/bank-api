const cardKey = (id) => `card-${id}`;
const userKey = (id) => `user-${id}`;
const transactionKey = (id) => `transaction-${id}`;
const chatKey = (id) => `chat-${id}`;

export const cacheHelper = { cardKey, userKey, transactionKey, chatKey };
